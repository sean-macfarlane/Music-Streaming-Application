<p id="notice"><%= notice %></p>

<h4><%= link_to '< Back', products_path %></h4>
<br />
<br />


 <div class="row featurette">
            <div class="col-md-4">
                <%=  image_tag(@product.image.url, class: 'media-object picture') %>
            </div>
            <div class="col-md-8">		
<h1>
  <%= @product.name %> 
</h1>

<p>
  <%= link_to @product.category.name, :controller => "categories", :action => "show", :id => @product.category.id %>
</p>
<p>
  <%= @product.description %>
</p>

<%= link_to "Play Song", @product.audio.url, :class => "play_song", remote:true%>
<br/>
<br/>
<% if current_user %>
<% if @product.user_id == current_user.id %>
<h4><%= link_to 'Edit', edit_product_path(@product) %></h4>
<% end %>
<%end %>




