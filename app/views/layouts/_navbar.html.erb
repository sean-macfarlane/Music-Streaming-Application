<nav class="navbar navbar-default" role="navigation">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
	  <%= link_to "Tuneify", root_path, :class => "navbar-brand"%>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul id = "categories" class="nav navbar-nav">
			<% Category.all.each do |c| %>
			<li><%= link_to c.name, :controller => "categories", :action => "show", :id => c.id %></li>
			<% end %>
		</ul>
		
		<ul class="nav navbar-nav navbar-right">
		<% if logged_in? %>
		  <% if current_user.isAdmin %>
		  <li><%= link_to 'Admin', categories_path %></li>
		  <% end %>
		  <li><%= link_to 'Upload', new_product_path %></li>
		  <li class="round-image-50"><%= image_tag(current_user.image.url) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li>
                <%= link_to "Log out", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
		</ul>
    </div><!-- /.navbar-collapse -->
</nav>